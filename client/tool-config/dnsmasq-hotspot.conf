interface=wlan0
bind-interfaces

# DHCP range (controller will rewrite these)
dhcp-range=192.168.4.50,192.168.4.150,255.255.255.0,12h

# Captive portal DNS wildcard (route every name to portal IP)
address=/#/192.168.4.1

# Reduce DNS weirdness
domain-needed
bogus-priv
